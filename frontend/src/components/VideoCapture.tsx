// This component is not actually used in the current implementation
// as video capture is handled directly in App.tsx
// Keeping it for potential future use

export default function VideoCapture() {
  return null;
}
